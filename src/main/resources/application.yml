spring:
  application:
    name: team3
  cloud:
    config:
      enabled: false  # Spring Cloud Config ????

  # H2 ?????? ??
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
    
  h2:
    console:
      enabled: true  # H2 ?? ??? (???)
      path: /h2-console
      
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop  # ???: ?????? ??? ??? ??, ??? ??
    show-sql: true  # SQL ?? ?? ??
    properties:
      hibernate:
        format_sql: true  # SQL ???

server:
  port: 8080

logging:
  level:
    org.depromeet.team3: INFO
    org.hibernate.SQL: DEBUG  # SQL ?? ??
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE  # SQL ???? ??
